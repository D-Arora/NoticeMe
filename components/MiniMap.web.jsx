import { View, Text } from "react-native";
export const MiniMap = ({ location }) => {
  return (
    <View>
      <Text>
        MiniMap not available on web. use native platform like iOS or andriod
      </Text>
    </View>
  );
};
